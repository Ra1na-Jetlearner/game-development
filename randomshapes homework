import pgzrun
import random

WIDTH=600
HEIGHT=800

TITLE="super Shapes"

def draw():
    for i in range(100):
        size=random.randint(5,8)
        r=random.randint(0,255)
        g=random.randint(0,255)
        b=random.randint(0,255)
        x=random.randint(50,550)
        y=random.randint(50,750)
        screen.draw.circle((x,y),size,(r,g,b))


pgzrun.go()


