import pgzrun
import random

WIDTH=850
HEIGHT=550

ball=Actor("balloon")
ball.pos = (random.randint(0,750), random.randint(0,450))

def draw():
    screen.fill(color=("pink"))
    ball.draw()

def update():
    pass
    # ball.x = random.randint (50,-50)
    # ball.y = random.randint (50,-50)

def on_mouse_down(pos):
    if ball.collidepoint(pos):
        print ("GOOD SHOT")
    else:
        print ("Try again")


pgzrun.go()