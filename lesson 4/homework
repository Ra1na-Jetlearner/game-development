import pgzrun
import random

WIDTH=800
HEIGHT=670

ITEMS=["blue-star","green-star","red-star","purple-star"]
main_item="yellow-star"
selected_item=[]

for i in range (5):
    random_item=random.choice(ITEMS)
    Blue=Actor(random_item)
    selected_item.append(Blue)
    Blue.pos=random.randint(0,800),random.randint(0,670)

def draw():
    screen.fill("pink")
    screen.blit("bground",(0,0))
    for actor in selected_item:
        actor.draw()

def update():
    pass


pgzrun.go()
